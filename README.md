# LLM Council - Совет Нейросетей

## Описание проекта

LLM Council - это веб-приложение, реализующее концепцию "Совета нейросетей", где несколько крупных языковых моделей (GPT, Gemini, Claude, Grok) работают совместно для предоставления более качественных и сбалансированных ответов на вопросы пользователей. Приложение использует 3-ступенчатый процесс:

1. **Сбор мнений** - каждая модель независимо отвечает на вопрос пользователя
2. **Взаимное рецензирование** - модели оценивают ответы друг друга
3. **Финальный синтез** - "председатель" (специальная модель) создает окончательный ответ на основе всех предыдущих этапов

## Архитектура с диаграммой потока

```
Пользовательский запрос
         ↓
[Frontend React/Vite] → [Backend FastAPI] → [PolzaAI API]
         ↓                      ↓                 ↓
     Отображение         3-ступенчатый      Запросы к
     результатов         процесс совета      LLM моделям
         ↑                      ↓
     Интерфейс с        Стадия 1: Сбор мнений
     анимациями         Стадия 2: Взаимное рецензирование
                        Стадия 3: Финальный синтез
```

### Бекенд (Python/FastAPI)

- **Фреймворк**: FastAPI
- **Порты**: 8000 (бекенд)
- **Ключевые файлы**:
  - [`main.py`](main.py:1) - основной сервер с CORS middleware и двумя endpoint'ами
  - [`services/council.py`](services/council.py:1) - реализация 3-ступенчатого процесса
  - [`services/polzaai.py`](services/polzaai.py:1) - клиент для API PolzaAI
  - [`services/config.py`](services/config.py:1) - конфигурация моделей и API

### Фронтенд (React/Vite)

- **Фреймворк**: React (TypeScript)
- **Порты**: 3000 (фронтенд)
- **Ключевые файлы**:
  - [`App.tsx`](App.tsx:1) - основной компонент приложения
  - [`services/geminiService.ts`](services/geminiService.ts:1) - сервис для взаимодействия с бекендом
  - [`constants.tsx`](constants.tsx:1) - константы для моделей
  - [`vite.config.ts`](vite.config.ts:1) - конфигурация Vite
  - [`types.ts`](types.ts:1) - TypeScript типы

## Установка

### Предварительные требования

- Python 3.8+
- Node.js 18+
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd <your-repo-directory>
```

2. Установите зависимости для бекенда:
```bash
pip install fastapi uvicorn httpx python-dotenv pydantic
```

3. Установите зависимости для фронтенда:
```bash
npm install
```

4. Настройте API ключ для PolzaAI:
   - Создайте файл `.env` в корне проекта
   - Добавьте ваш API ключ в формате:
     ```
     POLZAAI_API_KEY=ваш_ключ_здесь
     ```
   - Также ключ можно указать в файле `services/.env`

## 🔴 Запуск на домене (ДЕТАЛЬНО)

### Запуск бекенда

1. Перейдите в директорию проекта:
```bash
cd "c:/Users/user/Desktop/ГОТОВЫЙ Localhost ФОТО ЗАГОЛОВКИэ"
```

2. Запустите бекенд на порту 8000:
```bash
python main.py
```
или альтернативно:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Запуск фронтенда

1. В новом терминале перейдите в директорию проекта:
```bash
cd "c:/Users/user/Desktop/ГОТОВЫЙ Localhost ФОТО ЗАГОЛОВКИэ"
```

2. Запустите фронтенд на порту 300:
```bash
npm run dev
```

3. Проверьте, что фронтенд запущен:
   - Откройте браузер и перейдите по адресу: https://llm.clusterdev.ru
   - Вы должны увидеть интерфейс приложения LLM Council

### Проверка работоспособности системы

1. Убедитесь, что оба сервиса запущены:
   - Бекенд: https://llm.clusterdev.ru (должно отображаться сообщение о запуске)
   - Фронтенд: https://llm.clusterdev.ru (должен отображаться интерфейс приложения)

2. Протестируйте соединение между сервисами:
   - Введите тестовый запрос в поле ввода на фронтенде
   - Нажмите кнопку отправки
   - Должны начать поступать ответы от моделей в реальном времени

## Структура проекта

```
.
├── main.py                 # Основной сервер FastAPI
├── services/               # Сервисы для работы с LLM
│   ├── council.py          # Реализация 3-ступенчатого процесса
│   ├── polzaai.py          # Клиент для PolzaAI API
│   ├── config.py           # Конфигурация моделей и API
│   └── .env               # Файл с API ключом
├── App.tsx                # Главный компонент React
├── index.html             # HTML шаблон
├── index.tsx              # Точка входа в React приложение
├── vite.config.ts         # Конфигурация Vite
├── package.json           # Зависимости и скрипты фронтенда
├── tsconfig.json          # Конфигурация TypeScript
├── constants.tsx          # Константы для UI
├── types.ts               # TypeScript типы
├── src/
│   ├── assets/            # Статические ресурсы
│   └── styles/            # Стили приложения
└── README.md              # Документация проекта
```

## API документация

### Основные endpoint'ы

1. `GET /` - проверка работоспособности сервера
   - Ответ: `{"message": "LLM Council API is running!"}`

2. `POST /council` - синхронный запрос к совету
   - Тело запроса: `{"query": "ваш_вопрос"}`
   - Ответ: результаты 3-ступенчатого процесса

3. `POST /council/stream` - стриминговый запрос с Server-Sent Events
   - Тело запроса: `{"query": "ваш_вопрос"}`
   - Ответ: поток событий с промежуточными результатами

### Формат запроса

```json
{
  "query": "Ваш вопрос для совета нейросетей"
}
```

### Формат ответа (синхронный)

```json
{
 "opinions": [
    {"model": "model_name", "response": "ответ_модели"}
  ],
  "reviews": [
    {"model": "reviewer_model", "review": "рецензия_на_ответ"}
  ],
  "consensus": "финальный_ответ_председателя"
}
```

## Бекенд в деталях

### [`main.py`](main.py:1)

- **Назначение**: Основной сервер FastAPI
- **Основные функции**:
  - `/` - корневой endpoint для проверки работоспособности
  - `/council` - синхронный endpoint для получения результатов совета
  - `/council/stream` - стриминговый endpoint с Server-Sent Events
- **Ключевые строки**:
  - 29: Инициализация FastAPI приложения
  - 32-38: Настройка CORS middleware для разработки
 - 65-101: Реализация стримингового endpoint'а с SSE
  - 104-105: Запуск сервера на порту 8000
- **Как файл связан с другими**: Импортирует функции из [`services/council.py`](services/council.py:1) для обработки запросов
- **Что происходит при localhost запуске**: Сервер запускается на https://llm.clusterdev.ru, доступны все endpoint'ы

### [`services/council.py`](services/council.py:1)

- **Назначение**: Реализация 3-ступенчатого процесса совета
- **Основные функции**:
  - `stage1_collect_responses()` - сбор индивидуальных ответов
  - `stage2_collect_rankings()` - взаимное рецензирование
  - `stage3_synthesize_final()` - финальный синтез
  - `run_full_council_stream()` - стриминговая реализация всех этапов
- **Ключевые строки**:
  - 38-62: Стадия 1 - сбор индивидуальных ответов от моделей
 - 65-141: Стадия 2 - модели оценивают анонимизированные ответы друг друга
 - 144-203: Стадия 3 - председатель синтезирует финальный ответ
  - 584-632: Стриминговая реализация всего процесса
- **Как файл связан с другими**: Использует [`services/polzaai.py`](services/polzaai.py:1) для запросов к моделям и [`services/config.py`](services/config.py:1) для получения списка моделей
- **Что происходит при localhost запуске**: При получении запроса запускается 3-ступенчатый процесс, результаты возвращаются в формате, подходящем для фронтенда

### [`services/polzaai.py`](services/polzaai.py:1)

- **Назначение**: Клиент для API PolzaAI
- **Основные функции**:
  - `query_model()` - запрос к одной модели
  - `query_models_parallel()` - параллельные запросы к нескольким моделям
  - `query_models_stream()` - стриминг ответов от моделей
- **Ключевые строки**:
  - 8-68: Основная функция `query_model()` с обработкой ошибок
  - 75-98: Параллельные запросы к моделям
  - 101-130: Стриминговая реализация запросов
- **Как файл связан с другими**: Использует [`services/config.py`](services/config.py:1) для получения API ключа и URL
- **Что происходит при localhost запуске**: При запросах от бекенда отправляются HTTP запросы к API PolzaAI

### [`services/config.py`](services/config.py:1)

- **Назначение**: Конфигурационный файл
- **Основные функции**:
  - Хранение API ключа
  - Определение списка моделей совета
  - Определение модели председателя
- **Ключевые строки**:
  - 11: Загрузка POLZAAI_API_KEY из переменных окружения
  - 14-19: Список моделей совета
  - 22: Модель председателя
  - 25: URL API PolzaAI
- **Как файл связан с другими**: Используется [`services/polzaai.py`](services/polzaai.py:1) и [`services/council.py`](services/council.py:1)
- **Что происходит при localhost запуске**: Конфигурационные значения загружаются в память и используются для запросов к API

## Фронтенд в деталях

### [`App.tsx`](App.tsx:1)

- **Назначение**: Главный компонент приложения
- **Основные функции**:
  - Управление состоянием приложения
  - Отображение 3 стадий процесса совета
  - Обработка пользовательских запросов
  - Интеграция со стриминговым сервисом
- **Ключевые строки**:
  - 10-18: Определение начального состояния приложения
  - 36-135: Обработка отправки запроса и стриминг обновлений
  - 168-386: JSX разметка с анимациями и стилями
- **Как файл связан с другими**: Использует [`services/geminiService.ts`](services/geminiService.ts:1) для запросов к бекенду, [`types.ts`](types.ts:1) для типов и [`constants.tsx`](constants.tsx:1) для констант
- **Что происходит при localhost запуске**: Приложение загружается на https://llm.clusterdev.ru, пользователь может ввести вопрос и получить ответ через 3-ступенчатый процесс

### [`services/geminiService.ts`](services/geminiService.ts:1)

- **Назначение**: Сервис для взаимодействия с бекендом
- **Основные функции**:
  - `executeCouncilDeliberation()` - синхронный запрос к бекенду
  - `executeCouncilDeliberationStream()` - стриминговый запрос с SSE
- **Ключевые строки**:
  - 5-18: Синхронный запрос к `/council`
  - 32-39: POST запрос к `/council/stream` для стриминга
  - 45-158: Обработка Server-Sent Events потока
- **Как файл связан с другими**: Используется в [`App.tsx`](App.tsx:1) для выполнения запросов
- **Что происходит при localhost запуске**: При вызове функций устанавливаются соединения с https://llm.clusterdev.ru для получения результатов совета

### [`constants.tsx`](constants.tsx:1)

- **Назначение**: Константы для UI
- **Основные функции**:
  - Определение моделей и их стилей
  - Определение стадий процесса
  - Логотип Creomatica
- **Ключевые строки**:
  - 4-36: Определение моделей (GPT, GROK, GEMINI, CLAUDE) с цветами
  - 39-43: Определение стадий процесса
  - 45-73: Компонент логотипа
- **Как файл связан с другими**: Используется в [`App.tsx`](App.tsx:1) для отображения информации о моделях
- **Что происходит при localhost запуске**: Константы загружаются и используются для отображения UI элементов

### [`vite.config.ts`](vite.config.ts:1)

- **Назначение**: Конфигурация Vite
- **Основные функции**:
  - Настройка порта сервера
  - Настройка плагинов
- **Ключевые строки**:
  - 9-10: Настройка порта 3000 и хоста 0.0.0
  - 12: Подключение плагина react
- **Как файл связан с другими**: Используется Vite при сборке и запуске приложения
- **Что происходит при localhost запуске**: Приложение запускается на порту 3000

### [`package.json`](package.json:1)

- **Назначение**: Конфигурация проекта npm
- **Основные функции**:
  - Определение зависимостей
  - Определение скриптов запуска
- **Ключевые строки**:
  - 7-9: Скрипты запуска (dev, build, preview)
  - 11-16: Зависимости (react, react-dom, react-markdown и др.)
- **Как файл связан с другими**: Определяет зависимости для всего фронтенд приложения
- **Что происходит при localhost запуске**: При запуске `npm run dev` запускается Vite сервер на порту 300

## 🔴 Как работает на домене (ОЧЕНЬ ДЕТАЛЬНО)

### Полный процесс от запроса до ответа

1. **Инициализация приложения**
   - При запуске `npm run dev` Vite запускает сервер на https://llm.clusterdev.ru
   - При запуске `python main.py` FastAPI запускает сервер на https://llm.clusterdev.ru
   - CORS middleware на бекенде позволяет фронтенду взаимодействовать с бекендом

2. **Ввод запроса пользователем**
   - Пользователь вводит вопрос в поле ввода на интерфейсе (https://llm.clusterdev.ru)
   - При нажатии кнопки "Отправить" вызывается функция в [`App.tsx`](App.tsx:1)
   - Функция проверяет, что запрос не пустой и состояние приложения позволяет отправку

3. **Отправка запроса на бекенд**
   - [`services/geminiService.ts`](services/geminiService.ts:1) создает POST запрос к `https://llm.clusterdev.ru/council/stream`
   - Запрос отправляется в формате JSON: `{"query": "вопрос_пользователя"}`
   - Устанавливается соединение Server-Sent Events (SSE) для получения потока данных

4. **Обработка запроса бекендом**
   - FastAPI получает запрос на `/council/stream` endpoint
   - Вызывается функция `run_full_council_stream()` из [`services/council.py`](services/council.py:1)
   - Запускается 3-ступенчатый процесс обработки запроса

5. **Стадия 1: Сбор мнений**
   - Для каждой модели из `COUNCIL_MODELS` (Google Gemini, Claude, GPT, Grok) отправляется запрос
   - Запросы отправляются параллельно через [`services/polzaai.py`](services/polzaai.py:1)
   - Каждая модель отвечает на исходный вопрос независимо
   - Результаты по мере получения отправляются обратно клиенту через SSE

6. **Стадия 2: Взаимное рецензирование**
   - Модели получают анонимизированные ответы друг друга
   - Каждая модель оценивает качество, точность и полезность ответов
   - Рецензии по мере получения отправляются обратно клиенту через SSE
   - Ответы и рецензии анонимизированы для объективности оценки

7. **Стадия 3: Финальный синтез**
   - Председательская модель (Google Gemini) анализирует все ответы и рецензии
   - Создается финальный синтезированный ответ на основе всех предыдущих результатов
   - Финальный ответ отправляется клиенту через SSE

8. **Отображение результатов на фронтенде**
   - [`App.tsx`](App.tsx:1) получает события SSE по мере их поступления
   - Интерфейс обновляется в реальном времени по мере получения данных
   - Пользователь видит процесс на всех этапах: ответы моделей, рецензии, финальный синтез

9. **Завершение процесса**
   - После получения всех данных соединение SSE закрывается
   - Интерфейс показывает завершенный результат
   - Приложение готово к следующему запросу

### Особенности работы на production
- **Порты**: Бекенд на 8000, фронтенд на 3000
- **Соединение**: Фронтенд делает запросы к бекенду по https://llm.clusterdev.ru
- **CORS**: Middleware разрешает только production домен
- **Отладка**: В консоли браузера и терминале отображаются сообщения о процессе
- **Производительность**: На домене производительность зависит от сетевой инфраструктуры и доступности API


## Troubleshooting

### Типичные проблемы при запуске на домене

#### 1. Ошибка подключения к бекенду

**Симптомы**:
- Фронтенд не может отправить запрос
- Ошибка CORS или сетевая ошибка
- Сообщение "Failed to fetch"

**Решения**:
- Проверьте, запущен ли бекенд на https://llm.clusterdev.ru
- Убедитесь, что домен настроен корректно и доступен
- Проверьте настройки CORS в [`main.py`](main.py:1) (должны разрешать только production домен)
- Проверьте, что API ключ в `services/.env` действителен

#### 2. Ошибка с API ключом

**Симптомы**:
- Ошибки 401 или 403 при запросах к моделям
- Сообщения об аутентификации в логах
- Модели не отвечают на запросы

**Решения**:
- Проверьте файл `services/.env` и убедитесь, что ключ указан правильно
- Убедитесь, что ключ не истек и имеет необходимые права
- Проверьте, что переменная окружения `POLZAAI_API_KEY` доступна в процессе

#### 3. Ошибка с портами

**Симптомы**:
- Не удается запустить сервер
- Ошибка "Address already in use"
- Сервер не отвечает на ожидаемом порту

**Решения**:
- Проверьте, настроены ли доменные имена и SSL сертификаты корректно:
  ```bash
  curl -I https://llm.clusterdev.ru
  ```
- Проверьте доступность домена:
  ```bash
  ping llm.clusterdev.ru
  ```
- Или измените настройки домена в конфигурации:
  - Для бекенда: убедитесь, что сервер настроен на правильный домен и SSL
  - Для фронтенда: проверьте настройки прокси в `vite.config.ts` при разработке

#### 4. Ошибки зависимостей

**Симптомы**:
- Ошибки при запуске `python main.py` или `npm run dev`
- Сообщения об отсутствующих модулях
- Ошибки импорта

**Решения**:
- Для Python: `pip install fastapi uvicorn httpx python-dotenv pydantic`
- Для Node.js: `npm install` в корне проекта
- Убедитесь, что используете совместимые версии Python и Node.js

#### 5. Проблемы с SSE (Server-Sent Events)

**Симптомы**:
- Ответы не поступают в реальном времени
- Интерфейс не обновляется по мере получения данных
- Соединение обрывается до завершения

**Решения**:
- Проверьте браузерные инструменты разработчика (вкладка Network)
- Убедитесь, что endpoint `/council/stream` отвечает корректно
- Проверьте, что бекенд отправляет события в правильном формате
- Убедитесь, что не превышено время ожидания запроса

#### 6. Проблемы с отображением результатов

**Симптомы**:
- Интерфейс не обновляется после получения данных
- Результаты не отображаются должным образом
- Ошибки в консоли браузера

**Решения**:
- Проверьте консоль браузера на наличие JavaScript ошибок
- Убедитесь, что все зависимости React установлены корректно
- Проверьте, что типы данных соответствуют ожидаемым в компонентах

#### 7. Проблемы с производительностью

**Симптомы**:
- Медленный отклик приложения
- Долгое ожидание ответов от моделей
- Высокое потребление ресурсов

**Решения**:
- Проверьте подключение к интернету (запросы к API требуют стабильного соединения)
- Убедитесь, что API ключ имеет достаточный лимит запросов
- Проверьте системные ресурсы (оперативная память, процессор)

### Общие рекомендации по отладке

1. Проверьте логи в терминале для бекенда и в консоли браузера для фронтенда
2. Используйте встроенные инструменты разработчика в браузере для проверки сетевых запросов
3. Убедитесь, что все зависимости установлены и версии совместимы
4. Проверьте конфигурационные файлы наличие опечаток
5. Перезапустите оба сервера при возникновении неожиданных проблем
# 📋 ОТЧЕТ ОБ ОШИБКАХ ИЗ DOCKER LOGS

## Конкретные ошибки, которые мы ловили при запуске контейнера:

1. `"upstream" directive is not allowed here in /etc/nginx/conf.d/nginx.conf:4` -

2. `"server" directive is not allowed here in /etc/nginx/conf.d/nginx.conf:1` - 

3. `"events" directive is duplicate in /etc/nginx.conf:20` - дублирование блока events

4. `"user" directive is duplicate in /etc/nginx/conf.d/nginx.conf:1` - дублирование директивы user

5. `Restarting (127)` - контейнер постоянно перезапускался с кодом ошибки 127

6. `Restarting (1)` - контейнер постоянно перезапускался с кодом ошибки 1(после исправления со 127)

7. `"404 page not found"` - как следствие всех ошибок и постоянного restarting

Все эти ошибки были вызваны неправильной конфигурацией nginx и несоответствием архитектуры контейнера реальному проекту 